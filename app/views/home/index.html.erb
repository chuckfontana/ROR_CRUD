<div class="card card-body bg-light">
  <h4>
    The Universe is the totality of everything that exists, has existed, or ever
    will exist.
  </h4>
  <p>
    This Q&amp;A site is dedicated to questions about the history of the
    Universe
  </p>
</div>
<br />
<div class="container">
  <% @questions.each do |q| %>
  <div class="media">
    <div style="margin: 0 10px 0 0">
      <a href="#">
        <img class="media-object" src="<%= q.gravatar %>" />
      </a>
    </div>
    <div class="media-body">
      <div>
        <h4><%= q.email %> asked:</h4>
        <%= q.body %>
        <div><%= time_ago_in_words q.created_at %> ago</div>
      </div>
      <div style="margin: 8px 0 0 0">
        <a href="<%= question_path(q)%>" class="btn btn-success btn-xs"
          >View Answers</a
        >
      </div>
    </div>
  </div>
  <br />
  <% end %>
  <% if @questions.empty? %>
  <div class="alert alert-info">Nothing here yet, be the first to ask?</div>
  <% end %>
</div>
